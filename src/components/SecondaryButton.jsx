// Re-export so imports can read naturally: `import SecondaryButton from './SecondaryButton'`.
// The implementation lives alongside PrimaryButton since they share a base.
export { SecondaryButton as default } from './PrimaryButton'
